//Función que recibe dos parámetros para sumar y retornar el resultado

function suma(a, b) {
    return a + b;
}

//Función que reciba una cadena de texto que imprima en la consola el resultado

function imprimirCadena(cadena) {
    return console.log(cadena);
}

module.exports = {suma, imprimirCadena};
